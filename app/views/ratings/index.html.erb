<h2>List of ratings</h2>

<p><%= link_to 'Make a new rating', new_rating_path %></p>

<ul>
 <% @ratings.each do |rating| %>
  <li> <%= rating %> <%= link_to 'delete', rating, method: :delete, data: { confirm: 'Are you sure?' } %> </li>
 <% end %>
</ul>